<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hb.mapper.ShowyhplMapper">

	<!-- 查询评论信息 -->
	<select id="showyhpl" resultType="ShowyhplPojo" parameterType="int">
		select * from caipinpingjiabiao where caiDanID=#{caiDanId}
	</select>
</mapper>